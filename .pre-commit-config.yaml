repos:
  - repo: local
    hooks:
      - id: oxfmt
        name: oxfmt (format check)
        entry: npx oxfmt --check
        language: system
        pass_filenames: false
      - id: oxlint
        name: oxlint
        entry: npx oxlint
        language: system
        pass_filenames: false
